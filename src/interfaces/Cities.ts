
export interface Cities {
  main_text: string,
  secondary_text: string,
  temp: number,
  weather_description: string,
  temp_max: number,
  temp_min: number,
  lat: string,
  lon: string,
  isFavorite: boolean
}